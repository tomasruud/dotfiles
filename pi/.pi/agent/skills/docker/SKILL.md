---
name: docker
description:
  Manage Docker and Docker Desktop. Use when the user asks to start Docker,
  stop Docker, or manage Docker Desktop.
---

# Docker

## Start Docker Desktop

Run `docker desktop start` to start Docker Desktop. Wait for it to be ready
before running any docker commands.

```bash
docker desktop start
```
