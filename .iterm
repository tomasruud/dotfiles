#!/bin/bash

# adjust theme based on os settings
if [[ "$(uname -s)" == "Darwin" ]]; then
    if [[ "$(defaults read -g AppleInterfaceStyle 2>/dev/null)" == "Dark" ]]; then
        echo -ne "\033]50;SetProfile=Dark\a"
    else
        echo -ne "\033]50;SetProfile=Terminal\a" 
    fi
fi
