set drawbox
set hidden

cmd open $$VISUAL $fx

map m :push %mkdir<space>
map t :push %touch<space>

cmd delete %{{
	var d = (date "+%Y-%m-%dT%H-%M-%S")
	var dest = /tmp/lf-trash/$d
	mkdir -p $dest
	mv $E:f $dest
}}
map D :delete
