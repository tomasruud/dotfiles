set drawbox
set hidden

cmd open $$VISUAL $fx

map m :push %mkdir<space>
map t :push %touch<space>

cmd delete %{{
	d=(date "+%Y-%m-%dT%H-%M-%S")
	dest="/tmp/lf-trash/$d"
	mkdir -p $dest
	mv $f $dest
}}
map D :delete
