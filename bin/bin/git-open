#!/usr/bin/env janet

(use spork/sh-dsl)

(->>
  ($<_ git config --get remote.origin.url)
  (string/replace "https://" "")
  (string/replace "git@" "")
  (string/replace ":" "/")
  (string/replace ".git" "")
  (string "https://")
  ($ open))
